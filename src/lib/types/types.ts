export type Artilheiro = {
  jogador: string;
  time: string;
  gols: number;
};

export type Goleiro = {
  improvisado: boolean
  ativo: boolean;
  jogador: string;
  time: string;
  golsSofridos: number;
};
